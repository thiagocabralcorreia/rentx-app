import * as React from "react";
import { Container, Title } from "./styles";

interface HomeProps {}

export const Home = (props: HomeProps) => {
  return (
    <Container>
      <Title>HOME</Title>
    </Container>
  );
};
